<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
   <%= link_to "Foods", root_path,  class: "navbar-brand"%>
    <%# <a class="navbar-brand" href="#">Navbar</a> %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Public Recipes', public_recipes_path, class: "nav-link"%>
        </li>
        
        <li class="nav-item d-flex">
         <% if user_signed_in? %>
            <%= link_to 'General Shopping List', general_shopping_list_path, class: "nav-link"%>
          <%= link_to "Foods", root_path,  class: "nav-link "%>
     <%= link_to "My Recipe List", user_recipes_path(user_id: current_user.id), :class => 'nav-link' %>
         <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete }, class: "nav-link "%>
         <% else %>
         <%= link_to "Sign In", new_user_session_path,  class: "nav-link "%>
         <% end %>
          <%# <a class="nav-link" href="#">Link</a> %>
        </li>
      </ul>
    </div>
  </div>
</nav>
